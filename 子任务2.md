# 子任务2+3
项目位置：~/turtlebot3_ws/bot1_ws

## 安装 SLAM Toolbox

`cd ~/turtlebot3_ws/bot1_ws`

`source /opt/ros/humble/setup.bash`

`sudo apt update`

`sudo apt install ros-humble-slam-toolbox`

## 安装 TurtleBot3 键盘控制包
`sudo apt install ros-humble-turtlebot3-teleop`

⚠️ 一定要在新版 GazeBo 中启动

## 配置机器人
已写环境参数：  
`source ~/.zshrc`   

`source /opt/ros/humble/setup.bash`

`source ~/turtlebot3_ws/install/setup.bash`

`export LIBGL_ALWAYS_SOFTWARE=1`

`ros2 launch bot2 gazebo_bot2.launch.py`

传感器:

`source /opt/ros/humble/setup.bash`

`source ~/turtlebot3_ws/install/setup.bash`

`ros2 run ros_gz_bridge parameter_bridge \
/clock@rosgraph_msgs/msg/Clock[gz.msgs.Clock \
/scan@sensor_msgs/msg/LaserScan[gz.msgs.LaserScan \
/odom@nav_msgs/msg/Odometry[gz.msgs.Odometry \
/tf@tf2_msgs/msg/TFMessage[gz.msgs.Pose_V \
/imu@sensor_msgs/msg/Imu[gz.msgs.IMU \
/cmd_vel@geometry_msgs/msg/Twist]gz.msgs.Twist`


## SLAM建图
`source /opt/ros/humble/setup.bash`

`source ~/turtlebot3_ws/install/setup.bash`

`ros2 launch bot2 slam.launch.py`

## 键盘控制
`source /opt/ros/humble/setup.bash`

`source ~/turtlebot3_ws/install/setup.bash`

`export TURTLEBOT3_MODEL=burger`

`ros2 run turtlebot3_teleop teleop_keyboard`

### 自建 slam
source ~/turtlebot3_ws/install/setup.bash

colcon build --symlink-install \
  --packages-select bot2_simple_slam

ros2 run bot2_simple_slam simple_slam

#### 仿真时间修正
source /opt/ros/humble/setup.bash

source ~/turtlebot3_ws/install/setup.bash

ros2 param get /simple_slam 

use_sim_time

### GMapping
ros2 run slam_gmapping slam_gmapping --ros-args -p use_sim_time:=true

⚠️ 重新启动之前要删除旧进程

pkill -9 -f simple_slam

pkill -9 -f slam_gmapping

pkill -9 -f auto_explore

pkill -9 -f teleop_keyboard

pkill -9 -f parameter_bridge

pkill -9 -f gazebo

pkill -9 -f gz

pkill -9 -f rviz2

地图生成成功： ~/turtlebot3_ws/maps/bot2map.yaml

## Nav2导航
### 编译配置目录

cd ~/turtlebot3_ws

colcon build --symlink-install 
--packages-select bot2_nav2

### 启动
source /opt/ros/humble/setup.bash
source ~/turtlebot3_ws/install/setup.bash

ros2 launch bot2_nav2 nav2_bot2.launch.py

激活节点：

ros2 param get /lifecycle_manager_navigation autostart

确认导航栈的生命周期：


#ros2 lifecycle get /map_server
ros2 lifecycle get /amcl
ros2 lifecycle get /planner_server
ros2 lifecycle get /controller_server
ros2 lifecycle get /bt_navigator
ros2 lifecycle get /behavior_server## 地图设置
Rviz: Map --> Durability Policy --> Transient Local

启动前设置初始位置：

ros2 topic pub --once /initialpose geometry_msgs/msg/PoseWithCovarianceStamped \
"{header: {frame_id: map}, pose: {pose: {position: {x: 0.0, y: 0.0, z: 0.0}, orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}}}}"

## 自主导航（不要关Nav2终端）
cd ~/turtlebot3_ws

colcon build --symlink-install --packages-select bot2_nav2

source /opt/ros/humble/setup.bash

source ~/turtlebot3_ws/install/setup.bash

ros2 run bot2_nav2 multi_goal_nav