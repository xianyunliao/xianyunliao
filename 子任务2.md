# SLAM建图
## 时间同步的检验
启动 ROS-Gazebo 桥接  
`ros2 run ros_gz_bridge parameter_bridge /clock@rosgraph_msgs/msg/Clock[gz.msgs.Clock`  
检验（新终端）：  
`ros2 topic list`  
`ros2 topic echo /clock --once`  
临时启用设置  
`ros2 node list` 找到节点  
/ros_gz_bridge  
`ros2 param set /ros_gz_bridge use_sim_time true`  
检验时间同步  
`ros2 param get /ros_gz_bridge use_sim_time`  
 ## slam建图  
### 建立：
`mkdir -p ~/bot2_ws/src`  
`cd ~/bot2_ws`  
`mkdir -p ~/bot2_ws/maps`  
⚠️ 使用默认地图：  
`cp ~/turtlebot3_ws/src/turtlebot3/turtlebot3_navigation2/map/map.yaml \~/bot2_ws/maps/bot2_map.yaml`  
`cp ~/turtlebot3_ws/src/turtlebot3 turtlebot3_navigation2/map/map.pgm \~/bot2_wsmaps/bot2_map.pgm`   
`source /opt/ros/humble/setup.bash`  
`export TURTLEBOT3_MODEL=burger`  
`ros2 launch nav2_bringup localization_launch.py \map:=/home/xianyun/bot2_ws/maps/bot2_map.yaml \use_sim_time:=true`  

### gazebo设置迁移
`mkdir -p ~/bot2_ws/worlds`  
`mkdir -p ~/bot2_ws/launch`  

`cp ~/turtlebot3_ws/src/tb3_custom_world/worlds/custom_world.world \~/bot2_ws/worlds/custom_world.world`  

`cp ~/turtlebot3_ws/src/tb3_custom_world/launch/custom_world.launch.py \~/bot2_ws/launch/custom_world.launch.py`  
编译及检验：  
`cd ~/bot2_ws`

`source /opt/ros/humble/setup.bash`

`colcon build --symlink-install`

`source ~/bot2_ws/install/setup.bash`

`ros2 pkg list | grep bot2_world`

启动：

`source /opt/ros/humble/setup.bash`

⚠️必加：`source ~/turtlebot3_ws/install/setup.bash`

`source ~/bot2_ws/install/setup.bash`

`export TURTLEBOT3_MODEL=burger`

`ros2 launch bot2_world custom_world.launch.py`
### rviz同步
`source /opt/ros/humble/setup.bash`

`source ~/turtlebot3_ws/install/setup.bash`

`source ~/bot2_ws/install/setup.bash`

`ros2 launch nav2_bringup localization_launch.py \map:=/home/xianyun/bot2_ws/maps/bot2_map.yaml \use_sim_time:=true` 

### rviz2基本设置  
`mkdir -p ~/bot2_ws/rviz`  
`rviz2 -d ~/bot2_ws/rviz/bot2.rviz`  
map设定  
QoS Durability: Transient Local  
QoS Reliability: Reliable  
添加：map\LaserScan\TF\RobotModel

## SLAM建图
source /opt/ros/humble/setup.bash
source ~/turtlebot3_ws/install/setup.bash
source ~/bot2_ws/install/setup.bash

ros2 launch slam_toolbox online_async_launch.py use_sim_time:=true