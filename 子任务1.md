# 基于ROS2 Humble系统的 Gazebo 仿真环境搭建
⚠️ 此任务仍然需要在XQuartz terminal上运行
`ssh -Y xianyun@192.168.240.131`
`source /opt/ros/humble/setup.bash`

⚠️更优解：ubuntu 图形界面
`sudo apt update`
`sudo apt install -y ubuntu-desktop gdm3`
`sudo systemctl set-default graphical.target`
`sudo reboot`

## 安装 Gazebo Fortress
`sudo apt update`  
`sudo apt install -y ros-humble-ros-gz`  
测试 Gazebo 图形界面
`ign gazebo -v 4 shapes.sdf`

## 开始子任务1
### 检查环境配置
检查 Ubuntu 版本
`lsb_release -a`
检查 ROS 2 是否安装
`source /opt/ros/humble/setup.bash`  
`echo $ROS_DISTRO` --humble

