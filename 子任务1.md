# 基于ROS2 Humble系统的 Gazebo 仿真环境搭建
⚠️ 此任务仍然需要在XQuartz terminal上运行  
`ssh -Y xianyun@192.168.240.131`  
`source /opt/ros/humble/setup.bash`

⚠️更优解：ubuntu 图形界面
`sudo apt update`  
`sudo apt install -y ubuntu-desktop gdm3`  
`sudo systemctl set-default graphical.target`  
`sudo reboot`

## 安装 Gazebo Fortress
`sudo apt update`  
`sudo apt install -y ros-humble-ros-gz` 
安装ROS- GazeBoB集成库  
`sudo apt update` 
`sudo apt install -y \
  ros-humble-turtlebot3 \
  ros-humble-turtlebot3-description \
  ros-humble-turtlebot3-msgs \
  ros-humble-turtlebot3-teleop \
  ros-humble-xacro \
  ros-humble-robot-state-publisher \
  ros-humble-joint-state-publisher \
  ros-humble-sdformat-urdf \
  ros-humble-teleop-twist-keyboard \
  python3-colcon-common-extensions \
  python3-rosdep \
  python3-vcstool` 

测试 Gazebo 图形界面 
`ign gazebo -v 4 shapes.sdf` 

⚠️ 基于ARM64架构的 macOS 无法直接下载turtlebot3，需要使用源码下载
`cd ~/turtlebot3_ws/src/turtlebot3_simulations`  
确认 TurtleBot3 使用的是正确分支
`git branch --show-current` 
`feature-gazebo-sim-migration`  
`git fetch origin`  
`git checkout  feature-gazebo-sim-migration`    
`git branch --show-current`  
--feature-gazebo-sim-migration   
检查环境配置
`source /opt/ros/humble/setup.bash`  
`echo $ROS_DISTRO`  
`ros2 pkg list | grep '^ros_gz'`  
`ros2 pkg prefix ros_gz_sim`  
--/opt/ros/humble
`cd ~/turtlebot3_ws`  
`rm -rf build install log`  
`source /opt/ros/humble/setup.bash`  
`colcon build --symlink-install `  
`--packages-select turtlebot3_gazebo`  
`source install/setup.bash`  
`export TURTLEBOT3_MODEL=burger`  
最终安装
`cd ~/turtlebot3_ws`
`source /opt/ros/humble/setup.bash`  
`source install/setup.bash` 
`export TURTLEBOT3_MODEL=burger` 
`ros2 launch turtlebot3_gazebo`   `turtlebot3_world.launch.py`  


## 开始子任务1
### 检查环境配置
检查 Ubuntu 版本
`lsb_release -a`
检查 ROS 2 是否安装  
`source /opt/ros/humble/setup.bash`  
`echo $ROS_DISTRO` --humble  
检查 GazeBo 的配置
`cd ~/turtlebot3_ws`  
`pwd`  
`ls`  
### 仿真环境建立
脚本见：
Home/xianyun/turtlebot3_ws  
完成之后即在Ubuntu 终端机上用命令行控制  
启动GazeBo：  
`source /opt/ros/humble/setup.bash`  
`source ~/turtlebot3_ws/install/setup.`
`bash`  
`export TURTLEBOT3_MODEL=burger`  
`ros2 launch tb3_custom_world
custom_world.launch.py`  
### 命令行控制部分
通用检查：  
`source /opt/ros/humble/setup.bash`  
`source ~/turtlebot3_ws/install/setup.bash`  
`ros2 run teleop_twist_keyboard teleop_twist_keyboard`  
### 激光雷达发布
检测激光雷达：  
`gz topic -i -t /scan`  

